"""CLI entry (TODO-only scaffold).

Planned behavior:
- Parse CLI flags: --map, --algo [bfs|dfs|ucs|astar], --fog, --gui, --config.
- Load config.json defaults, override with CLI values.
- Headless: construct Grid and OnlineAgent, run, print metrics.
- GUI (optional): invoke visualizer loop.

Next steps:
- Implement load_config(), main(), and wiring to Grid/OnlineAgent/visualize.
"""

# TODO: implement CLI in main.py as per description above.
