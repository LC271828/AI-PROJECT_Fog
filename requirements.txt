# TODO: add runtime/testing dependencies when implementations are added.
# Suggested: pytest (for tests), pygame (optional GUI).
# Example (uncomment to pin):
# pytest==8.3.3
# pygame==2.6.1

# Core runtime deps for Fog Maze Game
pygame==2.5.2
