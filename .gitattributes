# Ensure consistent line endings across platforms
* text=auto

# Use LF for shell scripts so they run on Linux/macOS
*.sh text eol=lf

# Keep Python files normalized
*.py text eol=lf

# Allow Git to normalize others automatically
